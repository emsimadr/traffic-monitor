"""
BigQuery helpers (adapter / placeholder).

Today cloud sync is handled by `src/cloud/sync.py`. This module exists to align
the repo structure with the target architecture; we can progressively refactor
sync logic into here.
"""


